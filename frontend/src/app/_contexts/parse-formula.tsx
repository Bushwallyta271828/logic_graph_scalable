'use client';

export function ParseLogicalFormula({formula,substitutions}:{formula:string,substitutions:{[claimID:string]:string}}) {
  
}

export function ParseEquationSide({formula,substitutions}:{formula:string,substitutions:{[claimID:string]:string}}) {
  let first_plus = -1;
  let parenthetical_depth = 0;
  let i = 0;
  while 
  for (let i = 0; i < formula.length; i++) {
    
  }
}

export function ParseFormula({formula,substitutions}:{formula:string,substitutions:{[claimID:string]:string}}) {
  if (formula.includes("=")) {
    
  } else {
  }
}
